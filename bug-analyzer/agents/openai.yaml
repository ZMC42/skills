interface:
  display_name: "Bug Analyzer"
  short_description: "Analyze bug root causes and fix quality"
  default_prompt: "Use $bug-analyzer to require test steps, a precise bug timestamp, version anchors, logs, and tech stack up front; start from the fix commit's key changes, verify every conclusion against bug-version code or logs, and save the final root-cause report as a clearly named Markdown file."
